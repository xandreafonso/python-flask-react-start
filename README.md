# Projeto exemplo com Python, Flask no backend e React no frontend

No diretório frontend:

```shell
pnpm run build
```

Isso vai gerar a pasta dist.

No diretório raiz do projeto:

```shell
python3 app.py
```

